import logico.*;
public class Main {
    public static void main(String[] args) {
        /*Grafo grafo = new Grafo();

        Parada p1 = new Parada("A", "Centro");
        Parada p2 = new Parada("B", "Norte");
        Parada p3 = new Parada("C", "Sur");

        grafo.agregarParada(p1);
        grafo.agregarParada(p2);
        grafo.agregarParada(p3);

        grafo.agregarRuta(p1, p2, 10, 5, 2.5, 0);
        grafo.agregarRuta(p2, p3, 15, 7, 3.0, 1);
        grafo.agregarRuta(p1, p3, 25, 12, 4.5, 0);

        grafo.mostrarGrafo();
        */
    }
}